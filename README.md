# ng-contacts

A simple contacts manager with Angular 2

# Requirements
- NodeJS (>= 6.9)
- @angular/cli

# Installation
```
npm install
npm run build
```
And then open http://localhost:9484 in your browser
